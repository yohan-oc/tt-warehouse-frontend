name: Warehouse App

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

      - name: Tag and Push
        run: |
          VERSION=$(date +'%Y%m%d%H%M%S')
          git tag "v$VERSION"
          git push "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}" "v$VERSION"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create a Git Tag
        id: create_tag
        run: |
          VERSION=$(date +'%Y%m%d%H%M%S')  # Tag format like 20250129010101
          git tag "v$VERSION"
          git push origin "v$VERSION"  # Push the tag to GitHub

      - name: Upload Release Assets
        run: echo "Release assets have been uploaded!"

      - name: Deploy to GitHub Pages
        if: success()
        uses: peaceiris/actions-gh-pages@v3
        with:
          personal_token: ${{ secrets.GH_PAT }}
          publish_branch: gh-pages
          publish_dir: ./dist
          user_name: "github-actions[bot]"
          user_email: "github-actions[bot]@users.noreply.github.com"